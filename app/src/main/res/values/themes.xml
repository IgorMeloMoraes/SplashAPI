<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SplashAPI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SplashAPI" parent="Base.Theme.SplashAPI" />

    <!-- Tema da SplashScreen    -->
    <style name="Theme.SplashScreen" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Defini os atributos da splashScreen    -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowBackground">@drawable/pandaman</item>
        <item name="windowSplashScreenAnimationDuration">3000</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.SplashAPI</item>
    </style>
</resources>